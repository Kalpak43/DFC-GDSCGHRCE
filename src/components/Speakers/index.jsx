import React, { useState } from 'react'

export default function Speakers() {

    const [hideModal, setHideModal] = useState(true)

  return (
    // <div className="h-full w-full bg-red-500" onClick={() => setHideModal(x => !x)}>
    //     <div className={`fixed top-0 left-0 w-1/2 h-1/2 bg-green-400 ${hideModal ? "hidden" : ""}`}>

    //     </div>
    // </div>
    <div>
        Speakers
    </div>
  )
}
